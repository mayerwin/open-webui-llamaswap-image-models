# Normalize text to LF in the repo; force LF on checkout for scripts + sources
# so install.sh runs on Linux regardless of the cloner's autocrlf setting.
* text=auto
*.sh   text eol=lf
*.py   text eol=lf
*.md   text eol=lf
*.png  binary
